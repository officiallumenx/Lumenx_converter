export const SITE_MOTION_BOOT = `(function(){try{var r=document.documentElement;var reduce=window.matchMedia("(prefers-reduced-motion: reduce)").matches;r.classList.toggle("site-motion",!reduce);if(!reduce){document.querySelectorAll(".site-reveal").forEach(function(el){var b=el.getBoundingClientRect();if(b.top<innerHeight*0.92&&b.bottom>0){el.classList.add("is-in","is-instant");}});}}catch(e){}})();`;
