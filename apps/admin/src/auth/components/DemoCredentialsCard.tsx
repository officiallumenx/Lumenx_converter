/**
 * Demo credentials card — removed (Wave 1 API-only auth).
 * Kept as a no-op export so stray imports fail closed instead of showing secrets.
 */

/** @deprecated Demo Mode removed — renders nothing. */
export function DemoCredentialsCard() {
  return null;
}
