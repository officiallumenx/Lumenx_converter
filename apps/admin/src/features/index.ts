/**
 * Admin app feature map — structural entry for folder / import organization.
 * Runtime feature code remains under `components/` and `lib/`.
 */
export {
  ADMIN_FEATURE_BOUNDARIES,
  type AdminFeatureId,
} from "./boundaries";
